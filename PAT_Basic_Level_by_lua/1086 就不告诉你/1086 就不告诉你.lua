-- 题目编号: 1086 就不告诉你
-- 实现原理: 计算两数之积，将结果转为字符串后反转，去掉前导零后输出。若结果全为零则输出0。
-- 计算两数的乘积
local x = io.read("*n") * io.read("*n")
-- 将乘积转为字符串，反转，然后用gsub去掉开头的所有零
local s = tostring(x):reverse():gsub("^0+", "")
-- 如果结果为空字符串（说明全是零），输出0，否则输出反转后的结果
print(s == "" and "0" or s)
