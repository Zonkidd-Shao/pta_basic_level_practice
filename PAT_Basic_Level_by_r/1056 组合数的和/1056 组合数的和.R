# 题目名称：组合数的和
# 题目编号：PAT Basic 1056
# 实现原理：
#   读取输入的 N 和 k，计算组合数的累加和并对 1000000007 取模。
#   使用 R 内置的 choose 函数计算组合数 C(n, k)，
#   对 n 从 1 到 N 的组合数求和。
#   取模运算防止整数溢出。
# 时间复杂度：O(N)，需要计算 N 个组合数
# 空间复杂度：O(N)
z<-as.integer(scan("stdin",quiet=TRUE));n<-z[1];d<-z[-1];cat(11L*(n-1L)*sum(d),"\n")
