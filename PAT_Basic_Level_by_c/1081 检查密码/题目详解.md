# 1081 检查密码 详解

### 解题思路
- 逐字符读入一行密码（密码可能含空格，因此用 `getchar()` 逐个字符读到 `'\n'` 为止，而不能用 `scanf`）。
- 边读边统计四个指标：长度 `len`、是否有数字 `has_num`、是否有字母 `has_alpha`、是否含非法字符 `has_invalid`。
- 合法字符只有三类：数字（`isdigit`）、字母（`isalpha`）、点号 `.`；其余字符一律算非法。
- 判定按优先级顺序输出，只有前一条不满足才检查下一条：长度不足 6 → 含非法字符 → 缺数字 → 缺字母 → 全部满足。
- 细节：`scanf("%d", &n)` 后必须 `getchar()` 吃掉行尾换行，否则会被当成第一条密码。
- 时间复杂度：O(L)（L 为密码总长度）。

### 代码流程说明
- 读入密码条数 `n` 并 `getchar()` 清掉换行符。
- 外层循环 n 次：初始化 `len`、`has_num`、`has_alpha`、`has_invalid` 为 0。
- 内层 while 用 `getchar()` 逐个读字符直到换行：长度加 1，按 `isdigit` / `isalpha` / 是否为 `'.'` 更新对应标志。
- 按优先级 `if-else if` 链判断并输出对应英文提示，然后进入下一条密码。

### 代码流程图
```mermaid
flowchart TD
    Start["开始"] --> ReadN["读入密码条数 n，吃掉换行"]
    ReadN --> Loop{"还有密码 i < n ?"}
    Loop -- 否 --> End["结束"]
    Loop -- 是 --> Init["len=0, 各标志位清零"]
    Init --> ReadCh["getchar 读一个字符 c"]
    ReadCh --> IsNL{"c 是换行符？"}
    IsNL -- 否 --> Upd["len++, 按字符类型更新标志位"]
    Upd --> ReadCh
    IsNL -- 是 --> Check1{"len < 6 ？"}
    Check1 -- 是 --> Out1["输出 tai duan le"]
    Check1 -- 否 --> Check2{"has_invalid ？"}
    Check2 -- 是 --> Out2["输出 tai luan le"]
    Check2 -- 否 --> Check3{"!has_num ？"}
    Check3 -- 是 --> Out3["输出 needs shu zi"]
    Check3 -- 否 --> Check4{"!has_alpha ？"}
    Check4 -- 是 --> Out4["输出 needs zi mu"]
    Check4 -- 否 --> Out5["输出 wan mei"]
    Out1 --> Loop
    Out2 --> Loop
    Out3 --> Loop
    Out4 --> Loop
    Out5 --> Loop
```

### 解题流程图
```mermaid
flowchart TD
    Start["开始"] --> ReadPwd["读入一行密码"]
    ReadPwd --> Scan["扫描每个字符"]
    Scan --> Classify["分类：数字 / 字母 / 点号 / 非法字符"]
    Classify --> Done{"整行扫描完毕？"}
    Done -- 否 --> Scan
    Done -- 是 --> Prio{"按优先级逐条检查"}
    Prio --> L{"长度小于 6 ？"}
    L -- 是 --> P1["提示密码太短"]
    L -- 否 --> I{"含非法字符？"}
    I -- 是 --> P2["提示密码太乱"]
    I -- 否 --> N{"缺数字？"}
    N -- 是 --> P3["提示需要数字"]
    N -- 否 --> A{"缺字母？"}
    A -- 是 --> P4["提示需要字母"]
    A -- 否 --> P5["提示密码完美"]
    P1 --> More{"还有密码？"}
    P2 --> More
    P3 --> More
    P4 --> More
    P5 --> More
    More -- 是 --> ReadPwd
    More -- 否 --> End["结束"]
```
