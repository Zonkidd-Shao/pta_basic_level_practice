# 1121 祖传好运 详解

### 解题思路

- 题目定义"祖传好运"数：对数字的前 i 位（i 从 1 到 len），前 i 位组成的数都能被 i 整除。
- 一位数天然满足条件（1 % 1 == 0），可直接输出 "Yes"，无需进入判断循环。
- 判断方法：对每个 i（从 2 到 len），把字符串前 i 个字符逐位累加成整数 num，检查 `num % i == 0`；一旦某一位不整除就置 flag 为 0 并提前结束。
- 用字符串读入数字而非整数，可以避免大数溢出，也方便截取任意长度的前缀。
- 时间复杂度 O(len²)：每个 i 都要重新把前 i 位转成整数。

### 代码流程说明

1. 读入测试用例个数 K，进入 while(K--) 循环。
2. 每次读入数字串 s，取长度 len。
3. 若 len == 1，直接输出 "Yes" 并 continue。
4. 置 flag = 1；for 循环 i 从 2 到 len：内层 j 从 0 到 i−1 把前 i 个字符逐位拼成整数 num，若 `num % i != 0` 则 flag 置 0 并 break。
5. 按 flag 输出 "Yes" 或 "No"。

### 代码流程图

```mermaid
flowchart TD
    Start["开始"] --> Read["读入用例数 K"]
    Read --> Case{"还有用例？"}
    Case -- 是 --> Get["读入数字串 s"]
    Get --> Len1{"len == 1？"}
    Len1 -- 是 --> Yes1["输出 Yes"]
    Yes1 --> Case
    Len1 -- 否 --> Init["flag = 1"]
    Init --> Outer{"i 从 2 到 len？"}
    Outer -- 是 --> Build["把前 i 位转成整数 num"]
    Build --> Div{"num % i == 0？"}
    Div -- 否 --> Flag0["flag = 0，跳出"]
    Flag0 --> Output
    Div -- 是 --> Outer
    Outer -- 否 --> Output{"flag == 1？"}
    Output -- 是 --> Yes2["输出 Yes"]
    Output -- 否 --> No2["输出 No"]
    Yes2 --> Case
    No2 --> Case
    Case -- 否 --> End["结束"]
```

### 解题流程图

```mermaid
flowchart TD
    Begin["开始"] --> Input["输入数字串"]
    Input --> Special{"是一位数？"}
    Special -- 是 --> AllYes["直接判为好运数"]
    AllYes --> Finish["输出 Yes"]
    Special -- 否 --> Prefix["取前 i 位组成数"]
    Prefix --> Test{"能被 i 整除？"}
    Test -- 是 --> More{"i 是否到数字长度？"}
    More -- 否 --> Prefix
    Test -- 否 --> Bad["判定为不是好运数"]
    Bad --> Finish2["输出 No"]
    More -- 是 --> Good["所有前缀都满足"]
    Good --> Finish3["输出 Yes"]
```
