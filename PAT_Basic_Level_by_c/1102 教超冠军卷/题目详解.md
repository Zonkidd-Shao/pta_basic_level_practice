# 1102 教超冠军卷 详解

### 解题思路

- 题目要求在 N 件商品中分别找出"销量最高"和"销售额最高"的商品，本质是两次"最大值"统计。
- 无需存储全部商品：只需维护两个当前最优值（销量 max_sale、销售额 max_money）以及对应的商品编号，边读入边更新，空间复杂度 O(1)。
- 销售额需按"单价 × 销量"实时计算后参与比较，注意与销量冠军的更新是相互独立的两个过程。
- 编号用字符串保存，更新最大值时用 `strcpy` 拷贝即可。
- 时间复杂度 O(N)，只需一趟扫描。

### 代码流程说明

1. 程序首先读入商品总数 N，并初始化记录变量：sale_id、money_id 分别存放销量冠军与销售额冠军的编号，max_sale、max_money 初始为 -1（保证任意合法输入都能更新）。
2. 进入 for 循环逐一读入每个商品的编号 id、单价 price 和销量 count。
3. 先判断 `count > max_sale` 是否成立：成立则更新 max_sale 并把 id 复制到 sale_id，完成销量冠军的维护。
4. 再计算该商品销售额 `money = price * count`，若 `money > max_money` 则更新 max_money 与 money_id，完成销售额冠军的维护。
5. 循环结束后，先输出销量冠军 `sale_id max_sale`，再输出销售额冠军 `money_id max_money`。

### 代码流程图

```mermaid
flowchart TD
    Start["开始"] --> ReadN["读入商品总数 N"]
    ReadN --> Init["初始化 max_sale、max_money 为 -1"]
    Init --> Loop{"i < N？"}
    Loop -- 是 --> Read["读入编号 id、单价 price、销量 count"]
    Read --> C1{"count > max_sale？"}
    C1 -- 是 --> U1["更新 max_sale 并记录 sale_id"]
    U1 --> Calc["money = price * count"]
    C1 -- 否 --> Calc
    Calc --> C2{"money > max_money？"}
    C2 -- 是 --> U2["更新 max_money 并记录 money_id"]
    U2 --> Loop
    C2 -- 否 --> Loop
    Loop -- 否 --> Out1["输出销量冠军 sale_id 和 max_sale"]
    Out1 --> Out2["输出销售额冠军 money_id 和 max_money"]
    Out2 --> End["结束"]
```

### 解题流程图

```mermaid
flowchart TD
    Begin["开始"] --> Input["输入 N 件商品信息"]
    Input --> Track["扫描过程中维护销量最大值及销售额最大值"]
    Track --> Judge{"还有商品未处理？"}
    Judge -- 是 --> Compare["逐件比较并更新两个冠军"]
    Compare --> Judge
    Judge -- 否 --> Result["分别输出销量冠军与销售额冠军"]
    Result --> Finish["结束"]
```
